# My Express Template

## Installation

To clone the repo:

```ssh
$ git clone
```

Install the dependencies

```ssh
$ npm install
```

Run on dev mode:

```ssh
$ npm run dev
```

Run on production mode

```ssh
$ npm start
```
